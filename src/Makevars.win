# Makevars.win - Windows compilation flags for OpenMP support
# This enables OpenMP parallelization with Rtools on Windows
# Optimized for small matrices (<50 nodes) - focuses on -O3 and cache optimization

PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -O3 -funroll-loops -ffast-math
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Enable C++11 standard
CXX_STD = CXX11
